{
	"name": "camper.js",
	"version": "1.0.0",
	"description": "",
	"license": "AGPL-3.0-only",
	"author": "Ben Pate",
	"type": "module",
	"main": "camper.js",
	"scripts": {
		"build": "esbuild src/*.ts --bundle --format=esm --minify --outdir=./dist",
		"watch": "esbuild src/*.ts --bundle --format=esm --minify --outdir=./dist --watch",
		"deploy": "npm run build && mkdir -p ../emissary/_embed/templates/theme-global/resources/camperjs && find ../emissary/_embed/templates/theme-global/resources/camperjs -name '*.js.map' -delete && cp dist/*.js ../emissary/_embed/templates/theme-global/resources/camperjs/",
		"test": "vitest"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.27.3",
		"vitest": "^4.0"
	}
}
